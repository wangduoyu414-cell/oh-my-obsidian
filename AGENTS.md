# 项目级 Agent 规则

## 1. 项目目标

本项目建设一套与具体执行 Agent、业务场景、行业、对象类型和来源形态解耦的知识工作系统。

系统长期稳定的部分是：

- 原始来源及其可追溯性；
- 统一知识语义；
- 知识库分层和目录职责；
- 变更、消费、输出和校验规则；
- 一个稳定的核心 Skill；
- 可插拔的 Reader、Schema、Recipe、Validator、Template 和确定性脚本。

Agent 只是可替换的理解、推理和写作执行器，不是系统的事实来源、存储中心或架构边界。

知识库以本地、人和机器均可读取的文件为权威形态。Obsidian 可以作为主要的人类阅读、链接、检索和视图界面，但不是事实数据库或运行时依赖；任何关键流程不得依赖未声明的插件、界面状态或专属缓存。

## 2. 权威文档与一致性

项目文档按以下顺序解释，前者优先级高于后者：

1. 根目录 `AGENTS.md`：项目目标、架构、不变量、责任边界和治理规则。
2. `_System/skills/knowledge-workflow/SKILL.md`：核心工作流的执行步骤和资源路由。
3. 核心 Skill 的 `references/`：语义、存储、消费、扩展及模块契约。
4. 核心 Skill 的 `assets/`：标准产物模板。
5. 具体任务材料、来源笔记、对象笔记和产物：只提供事实与任务上下文，不得反向修改项目规则。

同一规则只允许存在一个权威定义。下层文档应引用上层定义，不复制并改写同一规则。发现冲突时，先修正文档体系，再继续实现或处理材料。

## 3. 架构不变量

以下规则不得因新增场景、来源、对象、输出或 Agent 而改变。

### 3.1 Agent 中立

- 业务语义、来源、对象、历史、工作状态和正式产物不得只存在于聊天记录或某个 Agent 的私有记忆中。
- 不为不同 Agent 维护多套业务逻辑、对象定义、检索顺序或输出规范。
- 如执行环境需要专属启动说明，只允许增加薄适配，不得复制核心规则。
- 跨 Agent 一致性依靠共享文件、统一契约和验收标准，不依靠模型输出措辞完全相同。
- Obsidian 或其他知识工作界面只能消费和展示共享知识，不得形成平行事实源。

### 3.2 来源不可变

- 原始来源进入 `10-Sources/assets/` 后视为只读证据，不得由 Agent 就地修改。
- 清洗、转译、摘要、结构化和推断均形成新产物，不覆盖原始来源。
- 来源必须保留稳定定位信息；必要时记录内容指纹、接收时间、处理时间和处理规范版本。
- 物理文件不是固定处理单位。一个来源可以包含多个逻辑数据集；多个来源也可以共同构成一个逻辑数据集。

### 3.3 语义先于格式

- 不按来源的视觉布局或字段顺序直接复制入库。
- 先判断来源角色和信息主体，再提取知识。
- 相同含义应落入同一语义；相同表述若主体、时间、范围或口径不同，不得错误合并。
- 不建设包含所有可能字段的万能结构。固定最小语义外壳，允许具体 Schema 按需要扩展。

### 3.4 主体、时间、范围和来源必须显式

任何能够影响当前知识、分析或正式产物的信息，至少必须表达：

```text
知识单元 = 主体 + 谓词 + 值或关联对象 + 时间范围 + 适用范围 + 来源定位 + 信息性质
```

缺少主体、时间、适用范围或来源的信息，不得静默写入当前知识；应保留为待核验项、来源摘要或任务提示。

### 3.5 事实、归纳和推断分离

信息性质至少区分：

- `source-stated`：来源明确表达，但尚未独立核验；
- `verified`：经指定权威来源或人工确认；
- `derived`：通过可复现规则或计算得到；
- `inferred`：Agent 基于现有信息作出的解释或判断。

`inferred` 不得覆盖前三类信息。`derived` 必须能说明推导依据。正式输出若使用未核验信息，必须显式标注其性质和限制。

### 3.6 先提议，后变更

- 核心工作流默认只生成变更建议，不直接覆盖知识对象。
- 对象识别、冲突处理、当前状态替换和删除必须经过明确审核，或由经过验证的确定性规则执行。
- 自动写入只有在目标、来源、规则、失败行为和回滚方式均已明确时才可启用。
- 原始事实的旧版本不得因当前视图更新而丢失。

### 3.7 精准消费

- Agent 不得无条件读取整个知识库、全部历史或全部来源。
- 消费前必须明确任务目标、对象范围、时间基准、适用范围和输出要求。
- 先精确定位，再扩展关联；先读取当前状态，再按需读取历史、事项、规则和来源。
- 所有复杂分析与正式输出优先基于受控的上下文包，而不是直接从原始来源临时拼接。

### 3.8 最小充分实现

- 优先复用现有契约、资源和脚本。
- 新需求应落在最小合适责任边界，不以新增完整 Skill 作为默认方案。
- 不为假设中的未来需求提前建设数据库、检索平台、编排层、自动化服务或多 Agent 系统。
- 只有已确认规模、权限、并发、性能、可用性或审计要求证明当前方案不足时，才升级基础设施。

## 4. 核心处理管线

项目只维护一条主线：

```text
来源材料
→ 识别逻辑数据集与来源角色
→ 形成规范化知识包
→ 生成来源笔记
→ 生成对象变更建议
→ 审核并更新知识
→ 按任务构建上下文包
→ 按 Recipe 生成产物
→ 校验、审核和归档
```

各阶段职责不得混淆：

- Reader 只理解来源结构并输出统一语义，不决定最终业务结论。
- Schema 定义对象身份、字段语义、来源权威性和合并规则，不负责读取来源或撰写产物。
- Recipe 定义产物目标、必需内容、组织结构和禁止项，不负责全库检索或补造事实。
- Validator 检查结构、完整性、一致性、可追溯性和确定性规则，不替代语义判断。
- Template 提供可复用表现形式，不拥有事实和业务逻辑。
- Script 承担重复、确定、易错或需要可复现的处理，不承担开放式判断。
- Skill 负责选择模式、组织步骤、路由资源和执行质量门，不容纳全部具体知识。

## 5. 最小知识模型

### 5.1 对象

对象是需要稳定身份、当前状态、历史和关联的知识主体。

- 对象必须有稳定内部标识。
- 外部标识、标准名称和别名只用于识别与匹配，不应混作同一字段。
- 只有当某个概念需要独立属性、历史、关系或任务时，才提升为对象；否则保留为普通值或链接。
- 不因来源中出现一行、一段或一个名称就无条件创建对象。

### 5.2 断言

断言表达某主体在特定时间和范围内具有某个值、状态或性质。

断言至少包含：主体、谓词、值、时间、范围、来源定位和信息性质。数值还应保留单位、精度、原始表达和统计口径；缺失项不得由 Agent 猜测。

### 5.3 事件

事件表达某个时间点或时间段内发生的变化。

- 事件以追加为主，不因当前状态变化而删除。
- 无法确定准确日期时，应保留已知粒度和不确定性。
- 当前状态可由事件和断言形成，但不得替代历史事件。

### 5.4 关系

关系表达对象之间的有向语义联系。

- 关系必须说明方向、关系类型、有效时间和来源。
- 名称相近、共同出现或语义相关不等于已建立关系。
- 关系可能随时间变化，不得默认永久有效。

### 5.5 事项

事项表达需要跟踪的需求、问题、风险、决定、承诺或行动。

事项应按需要记录负责人、状态、优先级、截止时间、依赖、完成证据和来源。未完成事项不得被叙述性摘要覆盖。

### 5.6 来源

来源既包括原始资产，也包括能够定位到原始资产内部的证据位置。

来源笔记是对逻辑数据集的稳定解释层；对象笔记是经审核后的当前视图。两者职责不同，不得互相替代。

## 6. 来源角色

处理前必须判断逻辑数据集的角色。最小角色集合为：

- `evidence`：可以提出事实或事件更新的直接证据；
- `snapshot`：特定时间和范围内的状态快照；
- `view`：由其他信息筛选、分类、聚合或展示形成的视图；
- `reference`：提供规则、定义、要求或解释依据；
- `superseded`：已被替代、撤销或仅用于历史追溯的内容。

`view` 默认不重复创建对象或覆盖事实。`snapshot` 只能在其时间和范围内解释。`superseded` 不参与当前状态更新，但必须保留历史价值。来源角色不能仅凭文件名决定。

## 7. 身份识别与合并

对象识别按以下顺序进行：

1. 稳定且明确的权威标识；
2. 已确认的系统内部标识；
3. 标准名称与已登记别名；
4. 多个属性的组合匹配；
5. 人工确认。

合并遵循以下规则：

- 新事件追加；
- 新关系在验证主体和方向后追加或更新有效期；
- 集合型属性合并去重；
- 当前状态只有在时间更晚、范围一致且来源适合时才可更新；
- 不同来源对不同谓词可以拥有不同权威性，禁止设置一个全局“最新来源覆盖一切”的规则；
- 冲突值并存进入待核验，不静默选择；
- 可再生摘要可以重建，原始事实及其来源不得丢失；
- 删除和失效以状态或事件表达，不以抹除历史表达。

## 8. 知识库结构

```text
/
├─ AGENTS.md
├─ 00-Inbox/
├─ 10-Sources/
│  ├─ assets/
│  └─ notes/
├─ 20-Knowledge/
│  ├─ objects/
│  └─ topics/
├─ 30-Work/
│  ├─ change-proposals/
│  ├─ context-packs/
│  └─ tasks/
├─ 40-Outputs/
│  ├─ drafts/
│  ├─ reviewed/
│  └─ final/
├─ 90-Archive/
│  └─ outputs/
└─ _System/
   └─ skills/
      └─ knowledge-workflow/
         ├─ SKILL.md
         ├─ agents/
         ├─ references/
         ├─ assets/
         └─ scripts/
```

目录职责：

- `00-Inbox/`：尚未识别、尚未登记或等待处理的材料；不得长期停留。
- `10-Sources/assets/`：只读原始来源，按接收批次或日期归档。
- `10-Sources/notes/`：按逻辑数据集生成的来源笔记、来源清单和处理状态。
- `20-Knowledge/objects/`：具有稳定身份的对象当前视图、历史、关系和来源链接。
- `20-Knowledge/topics/`：不依赖单一对象、可重复消费的稳定概念、规则和主题知识。
- `30-Work/change-proposals/`：尚未批准的新增、修改、冲突和失效建议。
- `30-Work/context-packs/`：围绕一个明确任务构建的受控消费包；可按保留策略清理。
- `30-Work/tasks/`：需要跟踪的工作状态、责任、依赖和完成证据。
- `40-Outputs/drafts/`：尚未审核的任务产物。
- `40-Outputs/reviewed/`：已经内容审核、但尚未成为最终版本的产物。
- `40-Outputs/final/`：明确批准的最终版本；不得用后续草稿覆盖。
- `90-Archive/`：已关闭、已替代或按策略转存的材料，不承担当前检索主路径。
- `_System/`：工作流规范、Skill、模板、脚本和校验资源，不保存任务事实。

Markdown 正文承载可读知识，YAML frontmatter 只保存身份、筛选、时间、范围、状态和生命周期等少量索引字段。不得把全部知识压入 frontmatter，也不得让界面生成的视图替代来源或对象笔记。

核心 Skill 内的扩展资源采用固定落位：契约保存在 `references/*-contract.*`；具体 Reader、Schema、Recipe 和 Validator 分别以 `references/reader-*`、`references/schema-*`、`references/recipe-*`、`references/validator-*` 命名；可复用表现文件位于 `assets/`；可执行确定性逻辑位于 `scripts/`。新增资源不得散落到知识库或任务目录。

## 9. 知识消费类型

系统支持五类消费，必须先确定类型再读取知识。

### 9.1 查询

针对明确问题返回有限答案。默认不形成长期产物；如答案需要复用、审核或追踪，应升级为上下文包或正式产物。

### 9.2 上下文包

为一个明确任务汇集最小充分信息。统一结构为：

```text
上下文包 = 任务目标
          + 对象与范围
          + 时间基准
          + 当前有效状态
          + 相关历史与关系
          + 未完成事项
          + 适用知识
          + 冲突、缺口与限制
          + 来源清单
```

上下文包不得复制完整知识库；只保留与当前任务有决策价值的信息。

### 9.3 产物生成

产物必须由 `Recipe + 上下文包` 生成。Recipe 控制结构和验收，上下文包提供事实边界。生成阶段不得绕过上下文包重新搜索全部原始来源，除非任务明确要求补充证据且补充结果先回到来源或上下文层。

### 9.4 知识维护

知识维护形成变更建议、冲突清单、失效建议或重新核验任务。维护结果在审核前不得被当作当前事实消费。

### 9.5 审计

审计检查来源、主体、时间、范围、信息性质、合并路径、产物引用和目录归类。审计可以发现问题，但不得在未授权时顺带修改内容。

## 10. 检索与上下文构建顺序

默认顺序为：

1. 精确定位目标和时间范围；
2. 读取目标当前视图；
3. 读取直接关系；
4. 按任务需要筛选相关事件；
5. 获取未关闭事项；
6. 获取适用主题知识和规则；
7. 汇总冲突、缺口、限制和待核验项；
8. 附上最小充分来源。

只有精确检索不能满足已确认需求时，才增加更复杂的索引或相关性检索。任何索引都是可重建缓存，不是事实来源。

## 11. 核心 Skill

项目只设一个稳定核心 Skill：`knowledge-workflow`。

它负责：

- 判断任务模式；
- 判断来源和目标的角色；
- 路由所需 Reader、Schema、Recipe、Validator、Template 和脚本；
- 执行来源、变更、消费、输出和审计的质量门；
- 将产物写入正确目录。

核心 Skill 不负责保存具体知识，也不应随着每个新场景频繁改写。新增能力优先通过资源扩展完成。

## 12. 扩展类型与路由公式

新增需求先回答“现有哪一层不足”，再选择扩展类型：

| 变化 | 应增加或修改的资源 |
|---|---|
| 新的来源结构或解释方式 | Reader |
| 新的对象语义、身份、字段、权威性或合并规则 | Schema |
| 新的产物目标、栏目、顺序或禁止项 | Recipe |
| 新的完整性、一致性、计算或质量要求 | Validator 或 Script |
| 新的可复用表现形式 | Template |
| 新的稳定全局原则或责任边界 | AGENTS.md，并同步检查核心 Skill |

新增完整 Skill 只在至少满足一项时成立：

- 存在独立且清晰的触发语义；
- 存在不同的权限或安全边界；
- 需要独立外部工具、服务或运行环境；
- 工作流足够复杂，加载后会显著污染核心 Skill 上下文；
- 具有明显不同的失败模式、验证方式或生命周期。

以下情况本身不足以创建新 Skill：

- 新增一种来源形态；
- 新增一种对象类型；
- 新增一种报告或输出；
- 新增一个部门、行业或业务场景；
- 仅希望更换提示词或措辞风格。

## 13. 新功能立项要求

任何新增功能、自动化或基础设施必须说明：

1. 目标和可观察完成标准；
2. 当前工作流的真实缺口；
3. 为什么较早的复用选项不能满足；
4. 应落入哪个责任边界；
5. 影响的来源、对象、消费方式和产物；
6. 新增权限、失败、恢复和兼容风险；
7. 确定性验证方法；
8. 开发与维护成本；
9. 回滚或停用方式。

如果缺口尚未由真实任务或运行证据证明，不进入实现。

## 14. 写入和产物生命周期

- Agent 只写入任务授权范围内的目录。
- 原始来源只移动归档，不修改内容。
- 来源笔记应稳定保存材料解释；对象笔记保存经审核的当前知识。
- Agent 生成内容与人工维护内容应使用明确章节或标记分隔，避免重建时覆盖人工内容。
- 临时规范化数据默认不长期保存；只有在复现、审计或性能需求明确时才持久化。
- 草稿、已审核版本和最终版本必须分目录，不以文件名后缀代替生命周期。
- `reviewed` 和 `final` 产物必须附带 manifest。
- 每个受控产物以独立目录保存其 manifest 和交付文件，避免相邻产物的来源、版本和审核状态混淆。
- 归档是状态变化，不应破坏来源链接和历史可追溯性。

## 15. 安全、权限与敏感性

- 来源、知识、上下文包、任务和产物应按适用制度记录敏感性和访问范围；没有既有制度时，不得由 Agent 擅自发明会产生权限承诺的分类。
- Agent、Reader、Script 和外部工具只获得完成当前任务所需的最小读取、写入、执行和网络权限。
- 未经明确授权，不得把受限内容发送到外部模型、服务、插件、接口或人员。
- 上下文包应优先最小化、去标识化或脱敏，不因调用方便复制无关敏感内容。
- 密钥、令牌、凭据和个人秘密不得写入 Skill、模板、来源笔记、对象笔记或产物正文。
- 日志和审计记录保存必要元数据，不无条件复制完整敏感内容。
- 权限变化、外部共享、自动发布和不可逆操作必须经过独立授权及人工确认。
- 敏感性和访问限制应沿“来源 → 知识 → 上下文 → 产物”传递；下游不得自行降低限制。

## 16. 校验与验收

每次相关变更至少检查：

- 目录和文件是否位于正确责任边界；
- 同一对象是否重复创建；
- 每条关键知识是否有明确主体、时间、范围、来源和信息性质；
- 当前状态更新是否遵守来源权威性和时间规则；
- 冲突是否显式保留；
- 上下文包是否最小充分；
- 产物是否只使用上下文包允许的信息；
- 产物关键结论是否可追溯；
- 新资源是否真的属于 Reader、Schema、Recipe、Validator、Template、Script 或独立 Skill；
- 更换执行 Agent 是否仍能得到相同的必需结构、事实边界和验收结果。

不得声称未执行的检查已经通过。

## 17. Agent 操作协议

### 开始前

1. 阅读本文件和适用 Skill。
2. 确认任务目标、授权范围、时间基准、权威来源和完成标准。
3. 检查当前目录、未完成变更和相关产物。
4. 判断任务属于摄取、维护、查询、产出、审计还是扩展。
5. 选择最小所需资源，不加载无关知识。

### 执行中

1. 保持来源只读。
2. 区分来源陈述、核验事实、确定性推导和 Agent 推断。
3. 先形成变更建议，再更新对象。
4. 发现关键歧义、身份冲突、时间冲突或权限扩张时停止写入并说明。
5. 不将聊天记录、临时上下文或模型记忆视为永久知识。

### 完成前

1. 执行相关 Validator 和脚本。
2. 检查来源、对象、上下文和产物之间的闭环。
3. 检查文档影响；若规则或结构变化，同步权威文档。
4. 报告实际执行的验证、未验证项和剩余风险。
5. 将产物放入正确生命周期目录。

## 18. 禁止事项

- 将某个 Agent、聊天客户端或模型服务作为唯一入口或永久依赖；
- 为每个场景复制完整工作流；
- 为每种来源或产物新建一个完整 Skill；
- 用一个超长提示词承载全部知识和规则；
- 将聊天历史当作知识库或正式档案；
- 将全部来源、历史和知识无差别放入模型上下文；
- 让 Agent 静默覆盖事实、解决冲突、删除历史或修改原始来源；
- 将分类视图、汇总视图或可重建索引当作独立事实来源；
- 在没有真实规模和质量证据时提前引入复杂基础设施；
- 在未经授权时自动执行外部写入、发送、审批、发布或不可逆操作。

## 19. 文档维护

- 改变项目目标、不变量、责任边界、目录职责或扩展门槛时，更新 `AGENTS.md`。
- 改变工作流步骤或资源路由时，更新核心 `SKILL.md`。
- 改变语义、存储、消费或模块契约时，更新对应 `references/` 文件。
- 改变标准产物结构时，更新对应 Template 或 Recipe，不在多个位置手工同步。
- 每次文档修改后检查术语、路径、模式名称和生命周期是否一致。

## 20. 完成定义

一项工作只有在以下条件全部满足时才算完成：

- 目标和授权范围内的产物已经生成；
- 来源未被破坏；
- 知识和产物可追溯；
- 冲突、缺失、推断和限制已披露；
- 目录归类正确；
- 相关验证已实际执行；
- 文档影响已处理；
- 没有把待审核内容冒充为当前知识或最终产物。
